-- ─────────────────────────────────────────────────────────────────────────────
-- Seed de datos — Hotel Blanca Patagonia
-- Se ejecuta con `supabase db reset`. Los datos del dominio (tipos de unidad,
-- temporadas, tarifas, política de cancelación) se cargan en la Fase 1.
-- ─────────────────────────────────────────────────────────────────────────────

-- (Fase 0) Sin datos todavía: la tabla `perfiles` se puebla automáticamente
-- mediante el trigger `al_crear_usuario` cuando se registran usuarios.
