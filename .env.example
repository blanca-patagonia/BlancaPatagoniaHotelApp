# ─────────────────────────────────────────────────────────────────────────────
# Variables de entorno — Sistema de Gestión Hotelera Blanca Patagonia
# Copiar este archivo como `.env.local` y completar con los valores reales.
# `.env.local` NO se versiona (está en .gitignore).
# ─────────────────────────────────────────────────────────────────────────────

# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL="https://TU-PROYECTO.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="tu-anon-o-publishable-key"

# Clave privilegiada (service role / secret). SOLO en el servidor. NUNCA exponer.
SUPABASE_SERVICE_ROLE_KEY="tu-service-role-key"

# ── Pagos (Fase 3) ───────────────────────────────────────────────────────────
# MercadoPago — https://www.mercadopago.com.ar/developers
MERCADOPAGO_ACCESS_TOKEN=""
MERCADOPAGO_WEBHOOK_SECRET=""
# Stripe — https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# ── Email transaccional ──────────────────────────────────────────────────────
# `resend` es el proveedor real; `consola` el simulador (escribe en el log y no
# envía). Los correos NO salen en el mismo request que los origina: se anotan en
# la bandeja de salida (`notificaciones`, migración 0075) y los manda el cron
# `/api/cron/notificaciones` con reintentos.
#
# ⚠️ `EMAIL_FROM` tiene que ser de un dominio VERIFICADO en Resend. Si no lo está,
# la API rechaza con 403 y el motivo queda en la bandeja.
RESEND_API_KEY=""
EMAIL_FROM="reservas@blancapatagonia.com"

# Secreto del webhook de ENTREGA (`/api/webhooks/email/resend`), que es lo que
# marca un aviso como entregado, leido o rebotado. Sale de Resend > Webhooks.
#
# ⚠️ Sin esto el sistema NO se entera de los rebotes: un correo queda en
# «enviada» para siempre, que se lee igual que «llegó». Un huésped con la
# dirección mal cargada figura avisado.
#
# ⚠️ Si falta, el handler responde 503 y no acepta ningún evento. Es a propósito:
# sin secreto no se puede distinguir un aviso de Resend de uno inventado, y
# cualquiera podría marcar como rebotados los correos del hotel.
RESEND_WEBHOOK_SECRET=""

# ── Variables que el código lee y no estaban declaradas acá ─────────────────
# Se detectaron en la auditoría del 2026-09: `grep process.env` encontraba ocho
# que este archivo no mencionaba, así que quien levantaba el sistema no tenía
# forma de saber que existían.
#
# Feeds iCal de Booking, pares `CODIGO_TIPO=url` separados por comas.
BOOKING_ICAL_FEEDS=""
# Simulador del asistente del portal. Vacío = simulado.
ASISTENTE_PROVIDER=""
# Firma del webhook del pago simulado. Sólo con `PAGO_PROVIDER=simulado`.
PAGO_WEBHOOK_SECRET=""
# Fuentes de cotización de divisas. Vacías = las públicas por omisión.
DOLARAPI_URL=""
ARGENTINADATOS_URL=""
# Entrada con Google en el login. `1` la habilita.
AUTH_GOOGLE_HABILITADO=""
# `1` vuelca el cuerpo de los correos al log. SOLO para depurar una plantilla:
# los cuerpos llevan enlaces con token.
EMAIL_LOG_CUERPO="

# ── Selección de proveedor (ADR 0018) ────────────────────────────────────────
# Cada integración externa elige su implementación con una de estas variables.
# En desarrollo se pueden dejar vacías: cae al simulador sin ruido.
# En PRODUCCIÓN son OBLIGATORIAS y el arranque falla si faltan o están mal
# escritas (`lib/integraciones/seleccion.ts`). Quedarse con el simulador es
# válido, pero hay que declararlo con el nombre del simulador, nunca por
# descuido: el de facturación emite un CAE inventado sobre una factura real.
EMAIL_PROVIDER=""         # simulador: "consola"  (escribe el correo en el log)
FIRMA_PROVIDER=""         # simulador: "local"    (sin validez legal)
FACTURACION_PROVIDER=""   # simulador: "simulado" (CAE simulado, NO es AFIP)
COTIZACION_PROVIDER=""    # respaldo:  "manual"   (usa la cotización que cargó un admin)
CANAL_PROVIDER=""         # simulador: "simulado" (no habla con ninguna OTA)

# ── WhatsApp (Cloud API oficial de Meta) ─────────────────────────────────────
# ⚠️ Este adapter es DISTINTO de los de arriba: si `WHATSAPP_PROVIDER` está
# vacía, el canal no se ofrece y todo sale por correo. No hay fallo al arrancar
# porque no hay nada que se pueda dar por enviado sin salir — la bandeja elige el
# canal ANTES de encolar (`canalDelAviso`).
#
# ⚠️ WhatsApp NO permite texto libre fuera de la ventana de 24 horas: hay que
# tener PLANTILLAS APROBADAS por Meta, una por evento, con el nombre y el orden
# de huecos que declara `lib/domain/whatsapp.ts`. Eso es un trámite del hotel con
# su cuenta de WhatsApp Business y no se puede resolver desde el código.
WHATSAPP_PROVIDER=""      # simulador: "consola" · real: "cloud_api"
WHATSAPP_TOKEN=""         # token PERMANENTE de usuario de sistema (el de prueba dura 24 h)
WHATSAPP_PHONE_NUMBER_ID=""
# Idioma de las plantillas aprobadas. `es_AR` y `es` son plantillas DISTINTAS
# para Meta: pedir una que no existe devuelve 132001 y el mensaje no sale.
WHATSAPP_IDIOMA="es_AR"

# `PAGO_PROVIDER` es el ÚNICO que admite varios separados por coma, porque el
# hotel ofrece varios medios a la vez: `mercadopago,stripe`. Stripe cubre la
# tarjeta internacional y MercadoPago los pesos, las cuotas y el efectivo.
# Simulador: "simulado". Con pasarelas reales van las credenciales de arriba.
PAGO_PROVIDER=""

# ── App ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# ── Sincronización automática de canales ────────────────────────────────────
# Secreto que autoriza a `/api/cron/canales`, el único endpoint que escribe en la
# base SIN sesión de usuario (corre con `service_role`, que saltea RLS).
#
# ⚠️ Si falta, el handler RECHAZA todo. No hay modo "sin secreto": eso lo
# convertiría en un endpoint público que escribe en la base, y el fallo sería
# silencioso justo en producción. Mismo criterio que el ADR 0018.
#
# Generar uno largo y aleatorio, por ejemplo con:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=""
